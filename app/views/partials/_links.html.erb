<%= content_tag :ul, '', class: 'links' do %>
	<% instance_variable_get("@#{type}").each do |link| %>
		<%= content_tag :li, '', class: 'link' do %>
			<%= link_to font_icon(link.icon) + link.name, link.href, title: link.title %>
		<% end %>
	<% end %>
<% end %>
